import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aprende-ionic-pages-routing-components-2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
